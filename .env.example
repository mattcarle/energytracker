# Copy to .env (gitignored) and fill in for a real deployment. If .env doesn't exist,
# docker-compose.yml falls back to "localhost", which is fine for local testing only.
SITE_ADDRESS=yourdomain.example.com
